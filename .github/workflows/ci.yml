# .github/workflows/ci.yml
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout du code
      - uses: actions/checkout@v3

      # Setup Node pour le frontend
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'  # active le cache uniquement pour npm
          cache-dependency-path: frontend/package-lock.json

      # Installer les dépendances frontend
      - name: Install Frontend Dependencies
        working-directory: frontend
        run: npm ci

      # Build frontend
      - name: Build Frontend
        working-directory: frontend
        run: npm run build

      # Lint frontend
      - name: Lint Frontend
        working-directory: frontend
        run: npm run lint

      # Test frontend (si tu en as)
      - name: Test Frontend
        working-directory: frontend
        run: npm test

    